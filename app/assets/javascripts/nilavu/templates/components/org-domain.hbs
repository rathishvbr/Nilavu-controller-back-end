<table class="table tb_list">
        <tbody>
            <tr>
              {{#each organizationType as |member|}}
                <td class="em_boarder_top">{{member.name}}</td>
                {{/each}}
            </tr>
        </tbody>
    </table>
